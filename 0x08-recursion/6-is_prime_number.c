#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * is_prime_number - function
 * @n: input
 * Return: 0,1
 */
int is_prime_number(int n)
{
	int d;
	if (d == 0)
		return (0);
	if 
		return (1);
	return (n % 2);
}
